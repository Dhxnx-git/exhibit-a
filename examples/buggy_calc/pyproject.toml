[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "buggy-calc"
version = "0.0.1"
description = "A deliberately buggy demo package for exhibit-a."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["buggy_calc*"]
