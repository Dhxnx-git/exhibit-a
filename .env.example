# Only needed for the LLM stages (reading issues, drafting tests).
# The validation engine — the part that decides CONFIRMED/UNREPRODUCIBLE —
# runs entirely offline and never sees this key.
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: override the default model for extract/synthesize.
# EXHIBIT_MODEL=claude-sonnet-4-6
